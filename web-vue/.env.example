# Copy to .env.local (gitignored) and point at the job-alerts Worker.
# Local dev: run `npm run dev` inside workers/job-alerts and use its printed
# http://localhost:8787 address. Production: the deployed Worker URL.
VITE_ALERTS_WORKER_URL=
